version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - HOST=0.0.0.0
      - PORT=8000
      - RELOAD=True
      - LOG_LEVEL=INFO
    env_file:
      - .env
    volumes:
      - .:/app
    command: python main.py
